module.exports = function(gulp, options, plugins){
    gulp.task('simulator', plugins.browserifyTask(gulp, {
        app: 'src/simulator',
        name: 'app',
        dest: options.simulatorDest
    }, plugins)); 
};