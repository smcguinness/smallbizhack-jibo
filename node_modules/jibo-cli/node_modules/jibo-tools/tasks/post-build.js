module.exports = function(gulp, options, plugins) {
    gulp.task('post-build', function(done){
        plugins.sequence(
            'simulator',
            'simulator-client', 
            'bin-build',
            'styles',
            'font-awesome',
            done
        );
    });
};