module.exports = function(gulp, options, plugins) {
    gulp.task('simulator-client-debug', plugins.babelTask(gulp, {
        src: ['src/simulator/client/**/*.js'],
        destDebug: 'client-debug',
        dest: options.simulatorDest
    }, plugins));
};