module.exports = function(gulp, options, plugins) {
    gulp.task('styles', function(done){
        return gulp.src(options.stylesSrc)
            .pipe(plugins.less())
            .pipe(plugins.minifyCSS())
            .pipe(gulp.dest(options.stylesDest));
    });
};