module.exports = function(gulp, options, plugins){
    gulp.task('simulator-client', plugins.browserifyTask(gulp, {
        app: 'src/simulator/client',
        name: 'client',
        dest: options.simulatorDest
    }, plugins)); 
};