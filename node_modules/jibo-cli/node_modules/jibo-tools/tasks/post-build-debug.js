module.exports = function(gulp, options, plugins) {
    gulp.task('post-build-debug', function(done){
        plugins.sequence(
            'simulator-debug',
            'simulator-client-debug',
            'simulator-launcher',
            'simulator-client-launcher',
            'bin-build-debug',
            'styles',
            'font-awesome',
            done
        );
    });
};