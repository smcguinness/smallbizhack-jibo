"use strict";

let jibo = require('jibo');

// Include custom classes here, additional as needed
let TestDebugBehavior = require('./behaviors/test-debug-behavior');

// Register the custom Behavior for the jibo runtime
jibo.bt.register(
    'TestDebugBehavior',
    '__package-name__',
    TestDebugBehavior
);